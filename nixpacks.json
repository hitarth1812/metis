{
  "phases": {
    "setup": {
      "nixPkgs": ["python311"]
    },
    "install": {
      "cmds": [
        "pip install --upgrade pip",
        "pip install -r requirements.txt"
      ]
    }
  },
  "start": {
    "cmd": "cd backend && gunicorn -k eventlet -w 1 --bind 0.0.0.0:$PORT --timeout 120 --log-level info wsgi:app"
  }
}
